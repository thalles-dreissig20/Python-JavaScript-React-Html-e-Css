// Arrays

let computador = ['Mouse','Monitor','SSD','Processador'];
let idades = [45, 55, 10, 20];

// A contagem dos valores começa em 0
console.log(computador[2]);

//descobrir o tamanho
console.log(computador.length);